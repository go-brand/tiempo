# Implementation Plans

Generated by the improve skill on 2026-06-25 (audited at commit `9f5bc98`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Load Temporal polyfill only when native Temporal absent | P1 | L | — | DONE (`51fa2c1`) |
| 002  | Stop `pnpm build` from writing into the user's home dir | P1 | S | — | DONE (`79d8115`) |
| 003  | Fix `format()` `x`/`X` offset tokens dropping offset minutes | P2 | S | — | DONE (`2978b98`) |
| 004  | Make `format()` AM/PM tokens locale-aware | P3 | M | — | DONE (`4485e26`) |
| 005  | `differenceIn*` truncate by default, opt-in `{ fractional }` | P2 | M | — | DONE (`fd61cc2`) |
| docs | Update MDX + regenerate skill refs/llms.txt for the above | — | S | 001-005 | DONE (`5f98de1`) |

All on branch `advisor/improve-all` (worktree). Reviewed & approved; **not merged** — awaiting user's merge decision.
Final state at tip `5f98de1`: `pnpm typecheck` exit 0, `pnpm exec vitest run` = 1464 pass (84 files), `pnpm exec tsup` emits a single dynamic polyfill import (0 static).

## Follow-ups (deliberately out of scope, flagged)
- **005 is breaking** (default of `differenceInDays/Weeks/Months/Years` flips fractional→truncated). Bump **minor or major** via `pnpm release`; note `{ fractional: true }` in release notes.
- Add a **Node 26** entry to `.github/workflows/ci.yml` matrix to exercise the native-Temporal path (CI currently only runs the polyfill branch).
- `installation.mdx` TypeScript section still phrases types as "from the polyfill" — accurate but dated.

Suggested order by leverage: **002** (fast, safe, removes a footgun) → **003**
(clear correctness bug) → **005** (API-consistency, breaking — land with a
version bump) → **001** (biggest win, largest change) → **004** (i18n polish).
All five are independent.

## Dependency notes

- No hard dependencies. 003 and 004 both edit `src/format.ts` but in different
  token blocks; run on separate branches, merge is trivial.
- 001 and 005 both touch the seven `differenceIn*` files (001 the import line,
  005 the signature/body). Non-conflicting; if both run, expect a trivial merge.
  See each plan's "Coordination" note.

## Decisions made

- **F1 contract**: opt-in fractional. Default = truncate toward zero (date-fns
  parity), `{ fractional: true }` returns precise values. Captured in plan 005.

## Findings considered and rejected

- "Trust-the-primitive" input errors (e.g. `toUtc(new Date('bad'))` throws
  RangeError): intentional per CLAUDE.md "no error handling for impossible
  states." Not a finding.
- `tiempo_banner.png` (5 MB) tracked in git: excluded from the npm package via
  `package.json` `files`, so it bloats only the repo, not consumers. Cosmetic;
  not worth a plan.
